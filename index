<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Thank You — Order Processing</title>
  <style>
    :root{--bg:#f7fafc;--card:#ffffff;--accent:#0b74ff;--text:#0f1724}
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",Roboto,"Helvetica Neue",Arial;
      background:linear-gradient(180deg,var(--bg),#eef2f7);
      color:var(--text);
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:24px;
    }
    .card{
      width:100%;
      max-width:720px;
      background:var(--card);
      border-radius:12px;
      box-shadow:0 8px 30px rgba(11,19,40,0.08);
      padding:32px;
      text-align:center;
    }
    .logo{
      display:inline-block;
      width:72px;
      height:72px;
      border-radius:12px;
      background:linear-gradient(135deg,var(--accent),#6fb1ff);
      color:white;
      font-weight:700;
      font-size:28px;
      line-height:72px;
      margin-bottom:16px;
    }
    h1{margin:6px 0 8px;font-size:22px;}
    p{margin:8px 0 16px;color:#374151;line-height:1.5}
    .meta{
      background:#f8fafc;
      border:1px solid #eef2f7;
      padding:14px;
      border-radius:10px;
      display:flex;
      gap:12px;
      align-items:center;
      justify-content:center;
      flex-wrap:wrap;
      color:#334155;
      font-size:15px;
      margin:16px 0;
    }
    .cta{
      display:inline-block;
      margin-top:10px;
      text-decoration:none;
      background:var(--accent);
      color:#fff;
      padding:10px 18px;
      border-radius:10px;
      font-weight:600;
      box-shadow:0 6px 18px rgba(11,116,255,0.18);
    }
    .small{font-size:13px;color:#64748b}
    a.contact{color:var(--accent);text-decoration:none;font-weight:600}
    footer{margin-top:18px;color:#94a3b8;font-size:13px}
    @media (max-width:420px){
      .card{padding:20px}
      h1{font-size:20px}
    }
  </style>
</head>
<body>
  <div class="card" role="main" aria-live="polite">
    <div class="logo" aria-hidden="true">✔️</div>
    <h1>Thank you for your purchase!</h1>
    <p>Your order is now processing. Delivery starts instantly and will complete within <strong>6 hours</strong>.</p>

    <div class="meta" aria-hidden="false">
      <div>
        <div class="small">Estimated completion by</div>
        <div id="eta" style="font-weight:700"></div>
      </div>
      <div style="border-left:1px solid #e6eef9;height:36px"></div>
      <div>
        <div class="small">Support</div>
        <div><a class="contact" href="mailto:gsnsolutions22@gmail.com">gsnsolutions22@gmail.com</a></div>
      </div>
    </div>

    <p class="small">If you face any problem or your order is not completed within 6 hours, please contact us at the email above — we are available 24/7 to help.</p>

    <a class="cta" href="#" onclick="window.close();return false;">Close</a>

    <footer>
      Note: Processing time may vary slightly depending on system load. Thank you for your purchase!
    </footer>
  </div>

  <script>
    // Show estimated completion time: now + 6 hours (client-side)
    (function(){
      const etaEl = document.getElementById('eta');
      function formatDate(d){
        // Example: Oct 24, 2025 — 11:45 PM (local)
        const opts = { year:'numeric', month:'short', day:'numeric', hour:'2-digit', minute:'2-digit' };
        return new Intl.DateTimeFormat(undefined, opts).format(d);
      }
      const now = new Date();
      const eta = new Date(now.getTime() + 6*60*60*1000);
      etaEl.textContent = formatDate(eta);
    })();
  </script>
</body>
</html>
